<?php

/**
 * Type of server this connection information is for.
 */
$type = "mysql";

/**
 * Server address.
 */
$host = "";

/**
 * Server database to connect to and interact with.
 */
$database = "";

/**
 * Server connection username.
 */
$username = "";

/**
 * Server connection password.
 */
$password = "";

/**
 * MySQL prefix that is attached to the beginning of each table name.
 * @var string
 * @remark Leave an empty string (NOT NULL) to not use a database prefix.
 */
$prefix = "";
